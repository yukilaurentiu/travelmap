<% if post.user.avatar.present? %>
  <%= image_tag(url_for(post.user.avatar), class: "avatar dropdown-toggle") %>
<% else %>
  <%= image_tag "avatar01.png", class: "avatar dropdown-toggle" %>
<% end %>
